# 12: Semantic And Hybrid Search

Status: in progress. Priority: P1. Owners: `search`, `vectorspace`, `engine`, `server`, `knowledgepolicy`.

Parity baseline: NornicDB [`main`](https://github.com/orneryd/NornicDB/tree/main) HEAD. References intentionally follow `main` rather than pinning a commit; re-check them against current HEAD whenever this plan is resumed. Treat every behavior or API difference as a parity defect until it is implemented or documented as strictly non-applicable; do not retain compatibility, legacy, or product-extension paths in this greenfield codebase.

## Objective

Implement one engine-owned semantic/hybrid search API using maintained lexical and vector indexes, deterministic RRF, policy filtering, hydration, pagination, cancellation, and stage diagnostics.

## Request And Response Contract

Requests identify database, mode (`lexical`, `semantic`, `hybrid`), text or vector, index/labels/fields, limit, offset, minimum score, RRF constant/weights, and caller policy context. Responses identify actual strategies, readiness, candidate/filtered counts, partial/degraded state, stable hits, and stage timings.

## Execution Order

1. Validate per-database feature gates and index readiness. NornicDB implementation: [`pkg/nornicdb/search_services.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/nornicdb/search_services.go), [`pkg/search/search.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search.go), and [`pkg/server/server_nornicdb.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/server/server_nornicdb.go). Reference tests: [`pkg/nornicdb/search_flags_e2e_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/nornicdb/search_flags_e2e_test.go), [`pkg/nornicdb/search_warmup_gate_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/nornicdb/search_warmup_gate_test.go), and [`pkg/search/index_flags_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/index_flags_test.go).
2. Produce bounded lexical and vector candidate batches independently. NornicDB implementation: [`pkg/search/search.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search.go), [`pkg/search/vector_pipeline.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/vector_pipeline.go), and [`pkg/search/vector_query_spec.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/vector_query_spec.go). Reference tests: [`pkg/search/search_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search_test.go), [`pkg/search/vector_pipeline_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/vector_pipeline_test.go), and [`pkg/search/search_vector_dims_more_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search_vector_dims_more_test.go).
3. Convert to one hit envelope and merge with deterministic RRF: score descending, stable ID tie-break. NornicDB implementation: `Service.rrfHybridSearch` and `Service.fuseRRF` in [`pkg/search/search.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search.go), plus outer chunk fusion in [`pkg/server/server_nornicdb.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/server/server_nornicdb.go). Reference tests: `TestRRFFusion`, `TestSearchService_RRFHybrid`, and `TestSearchService_HybridSearch` in [`pkg/search/search_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search_test.go), plus [`pkg/search/vector_id_collapse_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/vector_id_collapse_test.go).
4. Apply compliance/knowledge-policy/decay suppression before pagination. NornicDB implementation: [`pkg/search/decay_filter.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/decay_filter.go), `Service.filterByProperties` in [`pkg/search/search.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search.go), and [`pkg/knowledgepolicy/scoring_filter.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/knowledgepolicy/scoring_filter.go). Reference tests: [`pkg/search/decay_filter_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/decay_filter_test.go), [`pkg/search/filter_regression_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/filter_regression_test.go), and [`pkg/knowledgepolicy/scoring_filter_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/knowledgepolicy/scoring_filter_test.go). CopperDB must preserve its caller-specific compliance policy and add the corresponding request-envelope behavior; any different search visibility or ordering is a parity defect.
5. Hydrate only the retained bounded window and return diagnostics. NornicDB implementation: `Service.enrichResults`, `SearchResponse`, and `SearchMetrics` in [`pkg/search/search.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search.go), response mapping in [`pkg/nornicdb/db_admin.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/nornicdb/db_admin.go), and HTTP diagnostics in [`pkg/server/server_nornicdb.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/server/server_nornicdb.go). Reference tests: `TestSearchEnrichResultsBranches` and `TestSearchService_EnrichResults` in [`pkg/search/search_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search_test.go), [`pkg/nornicdb/search_mapping_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/nornicdb/search_mapping_test.go), and `TestHandleSearch` in [`pkg/server/server_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/server/server_test.go).

## Phases

1. Introduce shared index/service traits with status, generation, bytes, cancellation, and actual implementation kind. NornicDB implementation: `Service`, `CandidateGenerator`, and `ExactScorer` in [`pkg/search/search.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search.go) and [`pkg/search/vector_pipeline.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/vector_pipeline.go), with status ownership in [`pkg/nornicdb/search_services.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/nornicdb/search_services.go) and byte/strategy reporting in [`pkg/search/observability.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/observability.go). Reference tests: [`pkg/search/vector_pipeline_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/vector_pipeline_test.go), [`pkg/search/search_strategy_transition_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search_strategy_transition_test.go), and [`pkg/search/search_observability_extra_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search_observability_extra_test.go).
2. Implement local semantic batches over item 10 and optional item 11 query embedding. NornicDB implementation: `Service.VectorSearchCandidates` and `Service.vectorSearchOnly` in [`pkg/search/search.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search.go), indexed query resolution in [`pkg/search/vector_query_spec.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/vector_query_spec.go), and per-database query embedding in [`pkg/nornicdb/db_admin.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/nornicdb/db_admin.go). Reference tests: `TestSearchService_VectorSearchOnly` and `TestVectorQueryNodes_IndexedAndExactPaths` in [`pkg/search/search_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search_test.go), [`pkg/search/vector_pipeline_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/vector_pipeline_test.go), and vector-query embedding coverage in [`pkg/cypher/vector_procedures_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/cypher/vector_procedures_test.go).
3. Implement hybrid composition using existing RRF primitives and strict duplicate handling. NornicDB implementation: `Service.rrfHybridSearch` and `Service.fuseRRF` in [`pkg/search/search.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search.go), with optional lexical cluster routing in [`pkg/search/hybrid_cluster_routing.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/hybrid_cluster_routing.go). Reference tests: `TestRRFFusion`, `TestSearchService_RRFHybrid`, and `TestSearchService_HybridSearch` in [`pkg/search/search_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search_test.go), plus [`pkg/search/hybrid_cluster_routing_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/hybrid_cluster_routing_test.go) and [`pkg/search/vector_id_collapse_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/vector_id_collapse_test.go).
4. Replace the server's BM25-only branch with the engine API; maintain HTTP/embedded parity. NornicDB implementation: `Server.handleSearch` in [`pkg/server/server_nornicdb.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/server/server_nornicdb.go), canonical response conversion in [`pkg/nornicdb/db_admin.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/nornicdb/db_admin.go), and the gRPC adapter in [`pkg/nornicgrpc/search_service.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/nornicgrpc/search_service.go). Reference tests: `TestHandleSearch` and `TestHandleSearch_FiltersParameter` in [`pkg/server/server_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/server/server_test.go), [`pkg/nornicgrpc/search_service_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/nornicgrpc/search_service_test.go), and [`pkg/nornicdb/search_mapping_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/nornicdb/search_mapping_test.go).
5. Add parsed/query result caches keyed by data/index generation and stage-level observability. NornicDB implementation: `searchResultCache`, mutation invalidation, and `Service.Search` in [`pkg/search/search.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search.go), stage metrics in [`pkg/search/observability.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/observability.go), and spans in [`pkg/search/spans.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/spans.go). Reference tests: `TestSearchResultCacheHelpers` and `TestSearchCacheKeyAndMinSimilarityHelpers` in [`pkg/search/search_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search_test.go), [`pkg/search/search_metrics_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search_metrics_test.go), [`pkg/search/search_observability_extra_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search_observability_extra_test.go), and [`pkg/observability/catalog_search_test.go`](https://github.com/orneryd/NornicDB/blob/main/pkg/observability/catalog_search_test.go).

## Progress

- Complete: maintained vector indexes expose readiness, generation, strategy, dimensions, cancellation, file-backed exact reranking, and bounded ANN queries through the engine-owned manager.
- Complete: local semantic search queries every compatible declared node vector index, deduplicates stable entity IDs, applies minimum score before pagination, and returns deterministic ranked batches without record scans; persisted indexes rebuild into the same ranked results after reopen.
- Complete: local hybrid search produces bounded lexical and semantic batches independently and fuses duplicates through the shared deterministic RRF implementation.
- Complete: `POST /copperdb/search` is the CopperDB-branded equivalent of NornicDB's extension endpoint, and `POST /db/{database}/search` is the CopperDB database-scoped form. Both use the engine-owned pipeline and return the canonical node/score/RRF-rank array rather than a custom diagnostics envelope.
- Complete: HTTP search accepts a caller-provided `vector` without invoking the configured query embedder. A vector-only request performs semantic search, while non-empty text plus a supplied vector performs hybrid RRF; both retain canonical component ranks and actual-mode request telemetry even when embedding is disabled.
- Validated: public endpoint regressions persist a compatible three-dimensional vector index before engine construction, prove direct semantic retrieval through `/copperdb/search` and direct hybrid duplicate fusion through `/db/{database}/search` with `vector_rank` and `bm25_rank`, and reject a request with neither non-empty text nor a vector as `503` `unknown/error`. The hybrid case also verifies RRF input candidate telemetry and one `embedding`, `index`, and `hydration` duration sample under `mode: hybrid`.
- Complete: property filters use NornicDB semantics (AND across populated keys, OR within values, empty values ignored, scalar/array string matching) and restrict bounded lexical/vector candidates before source ranks, RRF, and final pagination.
- Complete: knowledge-policy visibility resolves from the durable catalog and suppresses lexical/vector candidates before source ranks, RRF, and final pagination, honoring created, version, last-accessed, and custom timestamp anchors plus compiled promotion predicates.
- Complete: caller-specific compliance checks deny explicitly requested governed labels/properties and suppress restricted candidate labels before ranks and pagination. Direct HTTP derives roles from its authenticated caller, while local ranked-search RPC validates the forwarded caller token to derive the same roles.
- Complete: search requests can select declared node FULLTEXT and VECTOR indexes through `indexes`; unknown or non-search selections fail validation, and the engine applies the same selected names to lexical, semantic, and hybrid candidate production.
- Complete: bounded local ranked-search batches are cached only after authorization and policy filtering. Cache keys include request inputs, normalized caller roles, durable data generation, and index-schema generation, so committed changes cannot return stale hits.
- Complete: canonical HTTP search responses remain result arrays while the server publishes catalog-backed request outcome, bounded candidate count, and embedding/index/hydration duration metrics with the same mode labels as the existing full-text procedure diagnostics.
- Validated: focused engine regressions cover compatible-index selection, semantic ranking and minimum score, stable labels/IDs, hybrid duplicate fusion, and pre-candidate cancellation/deadline handling; the engine suite passes 108/108, warning-denied Clippy passes for all engine targets, and workspace formatting is clean.
- Validated: server regression verifies a higher-ranked nonmatching lexical candidate is excluded before `limit: 1`, leaving the lower-ranked matching result through the canonical CopperDB routes.
- Complete: HTTP search matches NornicDB's `limit` behavior, defaulting omitted or zero limits to 10 while preserving explicit limits for the bounded engine pipeline.
- Validated: pre-cancelled and already-expired request contexts stop local semantic search before vector candidate work and preserve the typed `RequestCancelled` engine error. Ingress-wide cancellation ownership and mid-loop lifecycle coverage remain tracked by Plan 14.
- Validated: `crates/engine/benches/semantic_hybrid.rs` reproduces NornicDB's [`BenchmarkSearchProfile_RRFHybrid`](https://github.com/orneryd/NornicDB/blob/main/pkg/search/search_profile_test.go) fixture and operation boundary: 9,000 `Document` nodes, 64 dimensions, `title` plus `content` full-text index, deterministic sparse embeddings, limit 20, one warmup, and response/result cache disabled for the complete timed loop. Hybrid candidate budgets also match current NornicDB `main`: BM25 `max(limit * 2, 20)` and vector `clamp(limit * 10, 50, 5000)`.
- Validated: same-machine CPU-only upstream command `$env:NORNICDB_HNSW_BUILD_GPU_ENABLED = 'false'; go test ./pkg/search -run '^$' -bench '^BenchmarkSearchProfile_RRFHybrid$' -benchtime=1x -count=1` currently produces `1,014,200 ns/op` with HNSW `M=16`, `efConstruction=100`, and `efSearch=50`. CopperDB's current matching Criterion estimate is `1.0772 ms/op` (`[1.0726 ms, 1.0827 ms]`, 10 samples), approximately 6.2% slower. The host has measurable run-to-run variance, so retain production evidence whenever changing this pipeline.
- Validated: the production Criterion harness exposes a synthetic `rrf_hybrid_reopen` workload that persists, closes, and reopens CopperDB's 9,000-node fixture before one warmup search. Its first standard optimized run centered at `1.1077 ms` (`[1.0923 ms, 1.1256 ms]`) with 11 outliers; retain it as workload evidence rather than treating a single variable run as a new performance claim. This is not NornicDB's `BenchmarkSearchProfile_RRFHybrid_FromDisk`: current NornicDB `main` loads an external Badger/BM25/vector/HNSW production artifact at configurable `NORNICDB_PROFILE_DATA_DIR` with default 1,024 dimensions. That fixture is unavailable in this workspace, so the upstream benchmark currently skips and direct from-disk parity remains outstanding.
- Complete: the production harness includes `rrf_hybrid_high_limit` at `$k = 100$` alongside the upstream-equivalent `$k = 20$` baseline. It exercises the existing NornicDB-aligned fan-out policy at a larger bounded window: lexical `200` candidates and vector `1000` candidates, versus `40` and `200` at the baseline. The first standard optimized run centered at `2.7008 ms` (`[2.6482 ms, 2.7656 ms]`) with 10 outliers; retain this as bounded-window workload evidence, not a cross-implementation parity claim.
- Complete: the production harness includes `rrf_hybrid_cache_hit`, which enables and seeds CopperDB's generation-keyed ranked-search cache before the timed loop, then restores the disabled cache state. This isolates policy-safe steady-state cache-hit behavior from the intentionally uncached NornicDB-equivalent baseline. The first standard optimized run centered at `16.023 us` (`[15.980 us, 16.070 us]`) with two outliers; retain it as cache-hit workload evidence, not an uncached baseline comparison.
- Validated: ranked-search cache regressions cover a byte-for-byte cache hit, process reopen, data-generation invalidation after a node mutation, and independent index-schema-generation invalidation after adding an empty compatible vector index. The schema-only change preserves semantic results but correctly produces a cache miss.
- Complete: node full-text candidate generation follows NornicDB's warmed `FulltextIndexV2` fan-in/out model with write-invalidated compact `$u32$` document postings, per-document term frequencies, precomputed IDF, bounded prefix fan-out, cached query plans, suffix upper-bound pruning, deterministic ID ties, cancellation, and durable final hydration. Focused mutation, cancellation, and multi-property-ranking regressions pass.
- Complete: the hybrid request captures one generation-consistent knowledge-policy resolver, compliance-policy set, and validated index-definition set, then shares them across lexical and semantic candidate branches. This removes duplicate catalog generation checks and schema loads without weakening authorization, policy suppression, or selected-index validation. RRF deduplication uses `FabricGlobalId` keys with pre-sized bounded input storage while preserving stable-ID tie ordering and distributed diagnostics.
- Complete: engine-owned lexical candidate budgets now oversample filtered full-text requests with `max(limit * 2, 20)` before applying property filters and final pagination. The HTTP hybrid branch consumes the same engine-owned lexical fan-out rule, preventing the server from returning an empty page when a higher-ranked unfiltered candidate is excluded.
- Complete: HTTP search delegates local lexical, semantic, and hybrid composition to one engine-owned ranked-outcome API. The canonical result array keeps `bm25_rank` and `vector_rank` from the engine RRF merge, removing the server's duplicate hybrid fan-out and visibility handling.
- Complete: authorized HTTP search requests for a database that does not exist return NornicDB-compatible `404` before engine opening; authorization still runs first, so unauthenticated callers cannot use search to probe database existence.
- Complete: a database with no declared node FULLTEXT or VECTOR search index returns NornicDB-compatible `503` readiness diagnostics: its database name, `retryable: true`, and `request_status: "search_not_ready"`. In CopperDB's explicit-index model, creating a compatible index makes this state ready.
- Validated: HTTP search accepts an explicitly selected compatible index and rejects an unknown selection with `400`, rather than silently falling back to all indexes or reporting the database as not ready.
- Complete: the HTTP candidate gauge reports bounded source candidates before RRF duplicate collapse and final pagination (`input_hits`), not only the returned merged rows (`output_hits`); hybrid diagnostics therefore expose actual lexical/vector candidate work.
- Complete: HTTP search stage histograms export each `Instant` duration with sub-millisecond seconds precision, matching the full-text procedure and generic HTTP metrics instead of truncating fast embedding, index, or hydration stages to whole milliseconds.
- Complete: a public search request rejected before mode selection because no compatible index exists emits `nornicdb_search_requests_total` with `mode: "unknown"` and `result: "error"`; readiness failures are therefore observable without inventing a lexical or vector mode.
- Complete: an unknown or incompatible explicitly selected index is likewise counted as an `unknown/error` search request before mode selection, while successful selected-index searches retain their actual mode metrics.
- Complete: a vector-only request whose query embedding is unavailable returns its existing `503` and emits the same `unknown/error` request sample, because semantic mode cannot be selected without a query vector.
- Validated: the public vector-only disabled-embedding regression asserts the `503` no-query-vector response and its pre-mode `unknown/error` request sample.
- Complete: errors returned by the engine after HTTP mode selection are counted under that actual mode. The disabled-BM25 regression verifies the existing `503` configuration error emits `bm25/error`, rather than an ambiguous pre-selection label.
- Complete: database-info search status is derived from actual declared node FULLTEXT/VECTOR indexes rather than fixed placeholders. An online database with a compatible index reports `searchInitialized` and `searchReady`; one without such an index reports both false. `searchBuilding` remains false because CopperDB has no asynchronous search-build phase.
- Remaining: Plan 12 still requires broader parity evidence for the request/response diagnostics and production workload variation before its overall status can change. The current benchmark report identifies durable traversal and matched-quality HNSW retrieval as active performance risks; resolve or decompose those before making a general performance-parity claim.

## Tests And Performance

Test deterministic ties, dimensions, min score, fallback labels, disabled gates, declared indexes when automatic work is off, duplicate fusion, suppression before pagination, cancellation, restart/rebuild, and HTTP parity. Benchmark each stage, candidate oversampling, recall, QPS, hydration counts, and cache behavior.

## Definition Of Done

Semantic and hybrid paths no longer return unimplemented errors, use maintained indexes without unbounded hydration, produce deterministic policy-safe results, and expose actual strategy/readiness at engine and HTTP surfaces.

## Benchmark Parity Report

This report inventories every registered CopperDB Criterion benchmark as of this plan update and maps it to NornicDB `main` where an equivalent upstream benchmark exists. All CopperDB measurements below use Cargo's optimized benchmark profile on the same Windows host; NornicDB measurements use `go test -bench` on that host. Ratios are reported only where fixture shape and HNSW quality settings are sufficiently aligned.

| CopperDB target | Workload and current measurement | NornicDB `main` counterpart | Comparison status |
| --- | --- | --- | --- |
| `engine/semantic_hybrid` `rrf_hybrid` | 9,000 documents, 64 dimensions, limit 20: `1.0772 ms` | `BenchmarkSearchProfile_RRFHybrid`: `1.0142 ms` | Direct fixture/config match. CopperDB is approximately 6.2% slower. |
| `engine/semantic_hybrid` branch and control-plane probes | Lexical `590.12 us`; semantic `285.14 us`; storage fulltext `487.15 us`; RRF fusion `192.42 us`; cache hit `16.301 us`; isolated policy catalog load `9.0762 ms` | `BenchmarkBM25Search`, `BenchmarkVectorSearch`, `BenchmarkRRFFusion`, BM25 V2 disk benchmarks | Partial. The branch fixtures differ. The policy catalog probe is not on the generation-cached request path but is the largest local isolated probe. |
| `engine/traversal` `count_all_relationships` | 1,000 nodes, 999 edges, `MATCH ()-[r]->() RETURN count(r)`: `26.631 ms` | `BenchmarkMatchRelationships_CountAll`: `2.285 us` | Same query/cardinality, but CopperDB uses durable temporary storage while NornicDB uses a memory engine. This is a roughly 11,655x signal, not a language-only ratio; it is the highest-priority hotspot until a durable-versus-memory decomposition explains it. |
| `vectorspace/hnsw` default matrix | 10,000 vectors, $k=10$: ANN query `45.875 us`/`106.15 us`/`254.01 us` at 128/384/1024 dimensions; recall@10 `0.8375`/`0.7562`/`0.6625` | `BenchmarkHNSWIndex_Search`, `BenchmarkHNSWIndex_vs_BruteForce`, `BenchmarkHNSWRecall_ANNOnly`, `BenchmarkHNSWRecall_ANNWithRerank` | Partial. CopperDB defaults use `efConstruction=100`, `efSearch=50`; NornicDB's standalone HNSW benchmark defaults use 200/100, so raw default latency is not comparable. Recall falls below 0.8 at 384 and 1024 dimensions. |
| `vectorspace/hnsw` matched-quality 128D profile | 10,000 vectors, 128 dimensions, $k=10$, `efConstruction=200`, `efSearch=100`: ANN query `92.400 us`, recall@10 `0.9500` | `BenchmarkHNSWIndex_Search`: `72.925 us` | Same vector count, dimensions, $k$, and effort settings. CopperDB is approximately 26.7% slower; random vector/ID generation still differs, so treat this as a hotspot signal rather than a final cross-engine claim. |
| `search/lucene_fulltext` | Query parsing, candidate expansion, and one-document evaluation. At 2,048 terms: exact `443.03 us`, boolean `588.75 us`, wildcard `2.1264 ms`, regex `779.49 us`, fuzzy `11.635 ms` | `BenchmarkFulltextQueryParsing`, `BenchmarkBM25V2_SearchOnly_FromDisk` | No direct match. CopperDB measures Lucene-style query-language work; NornicDB's BM25 V2 benchmark measures an on-disk index. Fuzzy expansion is the largest local fulltext hotspot. The upstream BM25 V2 command skips here because its external disk fixture is unavailable. |
| `adminimport/offline_import` | 10,000 nodes plus 50,000 relationships. Best row throughput: ZIP/chunk 100,000 at `35.823 Krows/s`; CSV and gzip at that chunk size are `35.416` and `35.347 Krows/s` | None found in NornicDB `main` | CopperDB-only evidence. Increasing chunks from 1,000 to 100,000 improves throughput from roughly 11 Krows/s to 35 Krows/s. |

Traversal coverage now includes the upstream count-all shape, but CopperDB lacks counterparts for NornicDB's optional-match, chained-traversal, and shortest-path benchmark families. The next performance slice must first decompose the count-all traversal into query planning, edge enumeration, node hydration, and durable storage time; then match NornicDB's HNSW recall/effort configuration at 384 and 1024 dimensions before optimizing latency. Do not use unmatched fulltext, import, or default-HNSW numbers to claim cross-engine parity.